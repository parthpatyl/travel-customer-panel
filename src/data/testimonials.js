// Testimonials are fetched from /api/testimonials. When none are returned,
// TestimonialsSection renders an empty state — no hardcoded fallback here.
const testimonials = []

export default testimonials
